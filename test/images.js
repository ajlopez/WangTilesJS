const wt = require('..');exports['create image from plane'] = function (test) {    const plane = wt.plane(5, 5);    const image = wt.image(plane, 100);        test.ok(image);    test.equal(image.width(), 500);    test.equal(image.height(), 500);    test.equal(image.size(), 500 * 500 * 4);}